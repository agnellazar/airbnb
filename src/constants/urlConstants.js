import { suggestions_list } from "../stubs/suggestions";
import { feed_icons } from "../stubs/feed_icons";

export const urls = {
    feed_icons: "\\feed_icons",
    suggestions: "\\suggestions",
}

export const apiStubs = {
    "suggestions" : suggestions_list,
    "feed_icons" : feed_icons
}